<%- include('../includes/head.ejs') %>
</head>
<body>
  <%- include('../includes/nav.ejs') %> 
  </header>
  <main>
    <h1>List of products</h1>
    <% if (products.length > 0) { %>
      <% products.forEach(product => { %>
        <div class="ui card grid">
          <div class="image">
            <img src="<%=product.imageUrl%>" alt="A Book">
          </div>
          <div class="content">
            <h1 class="header"><%= product.title %></h1>
            <p class="meta">£<%= product.price %></h2>
            <p class="description"><%= product.description %></p>
          </div>
           <div class="">
            <button class="tiny ui basic orange button">
              <a class="btn" href="/product-list/<%= product._id %>">Details</a>
            </button>
            <%- include('../includes/add-to-cart.ejs', { product }) %>
          </div>
        </div>
      <% }) %>
    <% } %>
  </main>
<%- include('../includes/end.ejs') %> 
